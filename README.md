# 深海新绿 · 个人主页

一个零运行时依赖、以桌面窗口为核心交互的静态个人主页。所有个人资料集中在 `src/profile.js`。

页面使用保存在 `src/assets/fonts/misans` 中的 MiSans Web 字体中文子集，不依赖字体 CDN；相关许可文件与字体资源保存在同一目录。

## 本地运行

```bash
npm run dev
```

访问 <http://localhost:4173>。

## 构建

```bash
npm run build
```

构建结果位于 `dist/`。
